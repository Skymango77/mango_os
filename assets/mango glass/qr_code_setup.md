# [2026-02-03] π 결제 QR 시스템 구축

- **기능**: 사용자 결제를 위한 전용 QR 코드 모달 생성.
- **규격**: 지침에 따른 모달 너비 `92%` 적용 및 중앙 정렬.
- **보안**: `z-index`를 `10001`로 설정하여 부동산 모달보다 위에 뜨도록 설정.
- **디자인**: 망고 브랜드 컬러인 Yellow(π 컬러) 테두리 및 그림자 효과 적용.

구분,찾아야 할 위치 (VS Code),확인 사항
HTML,<body> 태그 바로 아래 #qr-modal 내부,QR 이미지 다음에 복사 박스가 있는지 확인
CSS/너비,"class=""... w-[92%] ...""",모달의 가장 바깥쪽 div에 92%가 적용됐는지 확인
Script,</body> 바로 위 <script> 태그 안,"copyWallet, requestConfirm 함수가 있는지 확인"

# [2026-02-04] 결제 완료 및 지갑 복사 시스템 구축

- **지침 준수**: 모든 결제 관련 UI 요소 너비 92% 적용 확인.
- **기능**: 파이 지갑 주소 원클릭 복사 로직 및 결제 확인 요청 알림창 구현.
- **UX**: 복사 -> 파이 앱 이동 -> 결제 -> 확인 요청으로 이어지는 시나리오 완성.

## 2026-02-04 UI 표준화 및 구조 최적화

### 1. 윈도우 규격 통일

- 모든 모달 및 카드 레이아웃의 너비를 `max-w-[480px]`로 고정.
- 모서리 곡률(`rounded-[2.5rem]`) 및 배경 블러 효과 통일.

### 2. 코드 구조 정렬

- 비정상적인 위치(Head/Body 사이)에 있던 모달 코드를 `<body>` 시작점으로 이전.
- `qr-modal`의 중복 코드 제거 및 내부 캡슐화 완료.

### 3. 향후 계획

- Pi Network 뉴스 윈도우의 높이 및 스크롤 속도 조정 예정.

## 2026-02-04 통합 인증 및 UI 버그 수정 내역

### 1. 모달 시스템 통합 (Integration)

- 기존 로그인 모달과 QR 결제 모달을 `integrated-auth-modal` 하나로 통합.
- 겹침 현상 해결을 위해 `z-index` 체계 정립 및 모달 위치를 `body` 최상단으로 고정.

### 2. Realty Portal 레이아웃 수정

- 매물명과 위치 정보가 우측 상단으로 튀는 버그를 `flex-col` 구조로 변경하여 텍스트 정렬 정상화.

### 3. 규격 표준화 (Standardization)

- 모든 팝업 윈도우의 가로 너비를 `max-w-[480px]`로 통일.
- 모서리 곡률(`2.5rem`) 및 배경 흐림 효과 수치 통일.
